version: '2'
volumes:
  db:
  phpmyadmin:
services:
  mariadb:
   container_name: dsijakSymfonyDb
   hostname: myhostname-db
   build: ./docker/db/
   image: mariadb
   environment:
     MYSQL_USER: dsijakSymfony 
     MYSQL_PASSWORD: dsijakSymfony
     MYSQL_DATABASE: dsijakSymfony 
     MYSQL_RANDOM_ROOT_PASSWORD: "yes"
   volumes:
     - db:/var/lib/mysql
  phpmyadmin:
   image: phpmyadmin/phpmyadmin
   container_name: phpmyadmin
   links:
    - mariadb:db
   depends_on:
    - mariadb
   environment:
    - PMA_ARBITRARY=1
   restart: always
   ports:
    - 8080:80
   volumes:
    - phpmyadmin:/sessions
  web:
    container_name: dsijakSymfonyWeb
    hostname: myhostname-www
    build: ./docker/www/
    depends_on:
      - mariadb
    links:
      - mariadb:db
    ports:
      - 7070:80 
    volumes:
      - ./project:/var/www
